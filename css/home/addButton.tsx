import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    addContainer: {
        backgroundColor: '#000',
        height: 60,
        width: 60,
        borderRadius: 30,
        alignItems: 'center',
        justifyContent: 'center',
        position: 'absolute',
        bottom: 50,
        right: 40,

    },
    add: {
        color: '#fff',
    }
});